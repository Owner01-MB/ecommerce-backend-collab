spring:
  application:
    name: backend

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ecommerce_backend
    username: spring_user
    password: sonab@26

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        open-in-view: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: snehajpatil624@gmail.com
    password: fejl hpca qvqd zmsz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

jwt:
  secret: Rfok+jCPCD3fR0Bau5FPRrt/+I5g8S+k37pdUWP/6m4=

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# default profile
---
spring:
  config:
    activate:
      on-profile: default
