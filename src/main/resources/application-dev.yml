spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_backend
    username: spring_user
    password: sonab@26

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        open-in-view: false

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  file:
    name: logs/ecommerce-app.log

jwt:
  secret: uC6HDLZloaChtEIxd4uYKWPDjX3S2mhUE0skNc60cW4=
